; SOL_ONLY

Lines that can cause bugs by being altered are denoted with // BUG, // Potential BUG, or // UNTESTED BUG

   Reachable memory leak (end of main)
 x Lost memory leak (no destructor on allocator)
 x Cctor missing (room)
   Return pointer to local variable
 x read past end of array
   use of uninitialized data
